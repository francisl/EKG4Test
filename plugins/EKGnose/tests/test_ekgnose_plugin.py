from unittest import TestCase
from EKGnose.plugin import EKGnose
from EKGnose.plugin import ReporterMessenger
#from mock import Mock, patch, create_autospec

class EKGnosePluginTest(TestCase):
    def test_plugin_should_start_at_zero(self):
        #with patch.object('EKGnose.plugin.ReporterMessenger', '__init__', return_value=Mock()):
            # p = EKGnose()
            # self.assertEqual(p.success, 0)
            # self.assertEqual(p.failures, 0)
            # self.assertEqual(p.errors, 0)
        ## self.assertFail()
        pass

